create a vertual env:
python -m venv .venv(what name u want to give)

activate venv:
.venv\Scripts\activate

downlode the requerments:
pip install -r requirements.txt

Modern Alternative of venv is UV

Go to python terminal 
and type -> import this
The Zen of Python, by Tim Peters